name: Docker build

on:
  push:
    branches:
      - main
jobs:
  builds:
    runs-on: ubuntu-latest

steps:
  - name: Checkout
    uses: actions/checkout@v4
  - name: Build docker image
    run: docker build -t nginx-site .